// import MusicCard from "./MusicCard";

// function MapCards(cards, handlePlay) {
//   return (
//     <>
//     useEffect(() => {
// {cards.map((card) => (
//         <MusicCard
//           cardId={card.id}
//           key={card.id}
//           image={card.album?.images?.[1].url}
//           songName={card.name}
//           artist={card.artists[0].name}
//           genre={card.genre}
//           onPlay={() => handlePlay(card.uri)}
//         />
//       ))}
//     }, [cards])

//     </>
//   );
// }

// export default MapCards;
